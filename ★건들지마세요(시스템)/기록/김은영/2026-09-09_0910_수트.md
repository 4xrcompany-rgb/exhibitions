---
작업자: 김은영
기획전: 0910_수트
날짜: 2026-09-09
상태: 진행중
소요:
단계:
  구성표: ok
  HTML: ok (post-1 스킨 fragment 2.5KB)
  배너: 규격 프레임 완료(제작은 ⑥ '기획전 디자인 끝' 확정 후)
오류:
---

> 👤 [[김은영]]  ·  🗂 [[0910_수트]]  ·  🏠 [[맵]]

## ✅ 잘된 점

- 2026-09-09 작업 시작. 게시판 = **post-1**(`boardid=post`) 로 확정받고 시작(§5-A-3 최우선 질문 통과).
- ⓪ 환경점검 통과 — 몰 200 / CDN 403(루트만, 파일경로 정상) / figma 404(엔드포인트 생존). 차단 아님, 로컬 실행.
- 폴더 상태 판정: `0910_수트/` 에 원본 엑셀(`0910_수트 기획전_황다빈.xlsx`)만 존재 → **①단계**부터 시작.
- 원본 엑셀 파싱 완료(`read_workbook.py`, 스테일캐시 회피). 시트 3개 — `코디 제품(보류)` / `기획전` / `상품리스트`.
  - 메인 타이틀 = "수트로 물들인 계절" · 기간 09.10-09.16 · 에디터 DB(황다빈)
  - 참여 브랜드 6개 = 323태그 / 뉴오브 / 러기드하우스 / 러기드옴므 / 트래드클럽 / 컴스페이스 / 바톨리니 (~81%)
  - 상품코드 134개 (상품리스트 A열)
  - `코디 제품` 시트는 **보류** 표기 → 반영하지 않음

## ⚠️ 막힌 점 · 오류

- 원본 엑셀 `참고링크` 칸(B28)이 **비어 있음** → 채팅으로 참고링크 수령: `read.php?index_no=21536&boardid=post` (post-1, 에어릿 26SS).
- 인앱 브라우저 스크린샷이 pane 미표시로 막힘 → **로컬 Playwright 로 우회 캡처**(scripts/_ref_shot.mjs 등). 실측 자체는 정상 수행.
- ★모바일은 데스크톱 768 축소가 아니라 **모바일 UA → m.4xr.co.kr 리다이렉트 + viewport 750 고정**. 768 로 요청해도 750 으로 렌더된다. 실측은 m 도메인에서 해야 정확.

### ② 참고 시안 21536 실측값 (post-1 최소형 확정)

- 에디터 `<style>` **1223자 한 덩어리뿐** · `main#intro` 없음 → §5-A-3 **최소형 순수 스타일 스킨** (21891·20461 계열).
- 스킨이 손대는 것: `.magazine_main_visual .txt_wrap{margin:100px 0;text-align:center}` · `.title{max-width:100%}` · `.magazine_view{margin:0 auto}` · `.hashtag_box li{h30/lh23/pad 5-17/fs14/radius30}` · 숨김(`.page_info` `.txt_03` `p.mb10` `.btn_board_like`) · `@768` 블록.
- **PC 1920**: 히어로 img 1905×683(`/files/board/21536/1776998896_2.jpg`) 풀블리드 → 타이틀 42px/lh50.4/#000/center → txt_02 16px/lh25.92 → 해시태그 6칩 → **탭(.navigation) li 2개** → `.goods_list_wrap` **1000px** · 상품 **4열**. docH 5920.
- **모바일 750**: 히어로 img **750×1000 별도 에셋**(`1776999452_3.jpg`) → 타이틀 50px → txt_02 28px/lh40.6 → 해시태그 li 55h/20px/pad16-30 → 탭 li 2개 각 375px/25px(활성=검정바탕 흰글씨, PC와 반전) → 상품 **4열**. docH 6727.
- → **KV 는 PC/모바일 2장 별도 제작 필요**(1905×683 · 750×1000).
- 상품카드·탭·해시태그는 전부 게시판 네이티브 렌더 → HTML 로 흉내내지 않음(admin 등록 대상).

### ② 재현 시안 제작 완료 (승인 대기)

- 방식: **참고본 21536 라이브 페이지에 우리 스킨을 주입하고 콘텐츠만 0910 수트로 치환 → Playwright 캡처.**
  로컬 저장본은 몰 CSS 컨텍스트가 깨져 부정확하므로 라이브 DOM 치환이 정확. 스크립트 `_소재/_작업파일/시안_렌더.mjs`.
- 치환: 타이틀·요약·기간·참여브랜드·에디터 = 엑셀 값 / KV = 규격 점선 자리표시(1905×683 · 750×1000) /
  상품 8개 = 엑셀 상품코드 앞 8개 실제 이미지·브랜드·상품명·판매가.
- ★가격 처리: 판매가는 네이티브 `strong.current_price`(자식 있음)라 leaf 필터로 안 잡혀 참고본 잔여 숫자가 남았었음 →
  **클래스 기준(`.current_price`/`.ori_price`/`.sale`)으로 교체**. 정상가·할인율은 진짜 취소선 근거가 없어 **비움**(가짜 할인 금지, §1-④).
  게시판이 AJAX 로 다시 채우므로 MutationObserver 로 유지.
- 납품용 스킨 초안 = `_소재/_작업파일/스킨_초안.html` (참고본 구조 그대로. 사용자가 "탭 CSS 필요없음" 이라 `.navigation` 규칙은 넣지 않음).
- 미확정: 해시태그 문구(엑셀에 없음 → 자리표시), 카테고리 탭 라벨.

### ⑤ KV 피그마 규격 프레임 생성 완료

- ★§7-7 확인: 세션 초기 "figma 인증 필요" 알림이 떴으나 **`whoami` 결과는 로그인 + Full seat** → 알림이 오탐.
  우회(HTML→JPG)하지 않고 **정상적으로 피그마에 규격 프레임 생성**. (2026-08-21 4XR데이 같은 사고 재발 없음)
- 파일: **https://www.figma.com/design/oZ9xbotQibFKrgjk9U1jr1** (`0910_수트 — 소재 규격`)
- 프레임 2개 — `kv_pc` 1905×683 (node 2:5) · `kv_m` 750×1000 (node 2:8).
  둘 다 FRAME + `clipsContent=true`(마스크) + `locked=true`, 점선 stroke, 프레임 **밖** 캡션(파일명·규격·저장폴더).
- 매핑 기록: `_소재/_작업파일/_figma_specs.json` · 폴더 안내: `_소재/01_KV/_규격안내.txt`
- 사용자 답변 반영: 탭 구성은 "CSS 필요없음" → 스킨에 `.navigation` 규칙 넣지 않음.
- 사용자 추가: "kv_pc, kv_m 없고 배너 이미지 전부 없음" → §1-⑤ 대로 **배너 규격 프레임까지 같은 피그마 파일에 생성**.
  `0910_suit_main_pc` 1870×462(4:5) · `0910_suit_main_m` 750×750(4:8) · `750610` 750×610(4:11) ·
  `0910_suit_600800` 600×800(5:4) · `0910_suit_app01/02` 800×464(5:10 / 5:13). 전부 locked+clip.
  ★카톡 600×800 은 §6 필수인 **하단 55px 바 포함** — 좌 "2026.09.10 - 09.16"(5:6) / 우 "UP TO 81%  ›"(5:7),
  둘 다 **텍스트 레이어 + locked=false** 라 사용자가 직접 수정 가능(하드코딩·이미지화 안 함).
  할인율 81% 는 원본 엑셀 참여브랜드 비고 "~ 81%" 에서 가져옴.
- 미정: 약칭 `suit` (임의 지정, 확인 필요) · 띠배너 2종(선택 규격) 필요 여부 · 앱푸시 장수(현재 2장).

### ③ 승인 → ④⑤ 완료 (2026-09-09)

- 사용자 **"완벽하게 구현됨"** 승인 → ④ 콘텐츠 반영 · ⑤ 구성표/소재/규격 진행.
- **위치안내 캡처** — 참고본 라이브에 스킨 주입 + 0910 콘텐츠 치환한 화면에 **구좌ID 라벨을 DOM 오버레이로 그려** 캡처
  (`_소재/_작업파일/위치안내_캡처.mjs`). PC/모바일 전체 + 상단 클립 4장 → `_소재/00_위치안내/`.
- ★**노출/숨김 실측 결과** (참고본 스킨 그대로 따른 결과):
  A01 KV·A02 제목·A03 요약·A04 해시태그·B01 탭·C01 상품 = **노출** /
  **A05 기간·A06 참여브랜드 = PC·모바일 둘 다 숨김**, A07 에디터 = PC 는 요소 자체가 없고 모바일은 숨김.
  → admin 에 넣어도 화면에 안 나오는 항목이라 구성표 가이드·⑥원본참고에 명시. 사용자 요청 시 숨김 해제.
- **구성표 엑셀 7시트 완성** (`기획전_구성표.xlsx`) — 체크리스트 전 항목 충족:
  ①사용설명 ②공통설정 ③섹션구성 21구좌 **+ 섹션 캡처 6장 임베드(O열)** ④상품코드 **134개**
  ⑤참여브랜드 7개 **몰 자동수집 + 전부 '일치' 검증** ⑥원본참고(링크+실측 근거) ⑦위치안내 **PC/모바일 캡처 임베드**.
  임베드 이미지 총 8장. 생성은 `make_planning_sheet.py`(표준 7시트) + 후처리 `구성표_채우기.py`(openpyxl 로 열어 바뀌는 칸만 수정, §7-1).
- ★브랜드 수집 함정: `brand_goods\.php\?brand=(\d+)[^>]*>(.*?)</a>` 정규식이 **BRAND PICK 슬라이더 전체를 한 링크로 삼켜**
  7개 중 4개가 같은 id(3)로 잘못 매칭됐다. → **브랜드명 문자열 위치에서 앞쪽 가장 가까운 brand= 링크**로 다시 매칭하고,
  각 brand_goods 페이지를 열어 `.title`/`.title_eng` 로 **검증**. 결과: 323태그=2 · 뉴오브=106 · 러기드하우스=3 ·
  러기드옴므=300 · 트래드클럽=309 · 컴스페이스=118 · 바톨리니=460, 7개 전부 일치.
- **납품본 `기획전.html` 생성** — post-1 최소형이라 `<style>` 한 덩어리(2.5KB).
  검증: 엔티티 0건(§7-9) · doctype/html/head/body 0건(fragment, §5-A-2①) · script 0개 · `@media(max-width:768px)` 하나만(§5-B).
  최상단 = `기획전.html` · `기획전_구성표.xlsx` · `_소재/` 3개만(§2).
- 최종 파일 기준 PC 1920 / 모바일 750 렌더 테스트 통과(CLAUDE.md ⑤).

## 💡 개선 요청 (중앙에 전달)

-
