---
작업자: 허숙현
기획전: 대형 기획전 자동화 (추석 기획전 타입)
날짜: 2026-09-18
상태: 성공
소요:
단계:
  구성표: 건너뜀      # 에디터 기획 엑셀('기획파일' 시트)을 그대로 읽음
  HTML: ok
  배너: 건너뜀
오류:
---

> 👤 [[허숙현]]  ·  🗂 [[대형 기획전 자동화]]  ·  🏠 [[맵]]

## 요청
- 사용자 : "추석 기획전 이제 자동화 만들자. '대형 기획전 타입 시작해줘' 하면 나오게."
- 사용자 선택 : ① 공통 파일 + 기획전별 설정 파일(무지성 방식) ② 에디터 기획 엑셀 그대로 읽기 ③ 이 PC 먼저 쓰고 검증 뒤 중앙에.

## 만든 것 — `E:\작업\콘텐츠\_대형기획전_자동화\`
- `engine/big_event.css`(97KB) · `engine/big_event.js`(82KB) : 추석 index.html 의 모양·기능을 떼어 낸 공통 파일 → CDN `2026/00/bigevent/{css,js}/`.
  기능 안의 추석 전용 값은 없었음(전부 맨 위 설정 블록) → 설정 블록만 `window.BF_EVENT` 를 읽게 바꿈.
- 기획전마다 : `settings_bigevent.js`(CDN 기획전 폴더) + `index.html`(게시판 붙여넣기 = 화면 틀 + 불러오는 줄 + 상품 검색 블록).
- `be.py init|make|check|render|all`
  - init : 폴더 뼈대 · img/ · 이미지_목록.md(설정이 쓰는 경로 29개 + 추석 크기) · 대형기획전.json(오픈일·폴더코드)
  - make : 기획 엑셀 → 설정 + index. C열 항목 이름으로 섹션을 찾음(쿠폰·상품권/예치금·럭키백·날짜표 2개·후기·로또·가족/가입). .xls·.xlsx 둘 다.
    엑셀에 없는 섹션은 통째로 뺌(스티키 탭·퀵 메뉴·로또 띠까지). 엑셀에 없는 값은 추석 값 + '확인 필요' 보고. 이미 파일이 있으면 `_자동생성/`.
  - check : 상품번호 살아 있는지 · 데일리 날짜가 기간 안 · 하루 12개 · 겹침 · 날짜 빠짐 · 이미지(폴더 img/ 또는 CDN)
  - render : 기획전 게시판 최근 글에 끼워 PC 1920 · 모바일 750 (서버에 아직 없는 공통 파일·설정·이미지는 로컬로 대신) → `_점검/render_*.png`
- `templates/markup.html` : 화면 틀, 글자 자리 46개 · 섹션 표시 20개. `samples/추석/` : 기본값(settings.json · texts.json · head.html).
- `tools/` : build_engine.py(완성 index → 공통 파일) · make_template.py(틀 → 템플릿, 되돌리면 원래와 같은지 확인) · settings_to_json.mjs · render.mjs
- 트리거 : 메모리 `big-event-automation`(부붐 스페셜이슈와 같은 방식 — 스킬 아님).

## 확인
- **공통 파일로 나눈 추석 = 원본** : 모바일 실측(비주얼 900 · 상품권 칸 2290 · 전체 13,508px · 글자 19.6px) 동일, smoke(모달 6 · 타이틀 애니 9/9 · 스티키 10 · 타이틀 이미지 9/9) PC·모바일 통과.
  PC 스냅샷 차이 5곳은 계속 움직이는 애니메이션 순간값. 첫 모바일 스냅샷 차이(비주얼 320 등)는 외부 CSS·이미지 로딩 전에 찍힌 가짜 → load 대기 후 동일.
- **추석 엑셀 → make = 추석 설정과 12곳만 다름** : 날짜 표기(같은 시각) · 비주얼 alt(에디터가 다듬은 문구) · 유의사항(추석 페이지에만 추가된 줄 : 로또 4 · 후기 1 — make 가 목록으로 알려 줌) ·
  퀵 이름(섹션 제목으로) · 전 상품 보기 링크(게시글 번호라 비움). 화면 틀 차이는 에디터가 다듬은 문구뿐.
- **섹션 빼기(로또·럭키백 없는 xlsx)** : 섹션 7 · 스티키 8 · 퀵 5 · 로또 띠 없음 · JS 오류 0(PC·모바일).
- **추석 엑셀 → make → render** : 섹션 높이 PC(쿠폰 535 · 상품권 738 · 럭키백 1126 · 데일리 1324/1354 · 후기 742 · 로또 2038 · 랭킹 1181 · 회원 541)·모바일 모두 실제 추석 페이지와 같음 · 깨진 이미지 0 · 가로 넘침 0 · JS 오류 0.
- **check** : 상품번호 340개 전부 살아 있음 · 데일리 14일 × 12개 · 이미지 전부 있음 → 문제 없음.

## 테스트 중 고친 것
- 상품권 문구에 대사가 아닌 줄("필요할 때마다 꺼내 쓰는 할인 예치금")이 섞임 → 말하는 사람(이모:·고모:) 줄만 읽게.
- 쿠폰 유의사항 '마이페이지 > 쿠폰' 링크가 빠짐 → 같은 문장이면 기본값(링크 포함)을 쓰게.
- 로또가 없을 때 퀵 메뉴 아래 로또 띠가 남음 → 띠도 로또 표시로 묶어 같이 빠지게.
- 이미지 목록이 추석 로컬 폴더 구조로 나옴(giftcard/ ← 실제는 giftcard/id/, 섹션 제목은 pd/title/) → 설정이 실제로 쓰는 경로로.

## 남은 것 · 다음
- 공통 파일(engine/big_event.css · big_event.js)은 **아직 CDN 에 안 올림** — 첫 대형 기획전 때 `2026/00/bigevent/css/` · `/js/` 에 올림.
- 사용자 요청(2026-09-18) "어느 PC 든 사용 가능하게" → 도구를 어느 PC 에서나 돌게 고침(E: 경로 제거 · 이미지 크기 표 동봉 · xlrd 없으면 자동 설치) 후 GitHub 브랜치로 중앙 전달(아래 💡).

## 💡 개선 요청 (중앙에 전달)

- ★**대형 기획전 자동화 스킬을 main 에 합쳐 주세요** — GitHub 브랜치 `feature/big-event-automation` (커밋 6636c91, 파일 18개 · `.claude/skills/big-event-automation/` 한 폴더만 추가, 기존 파일 수정 없음)
  - 합치면 모든 PC 에서 **"대형 기획전 타입 시작해줘"** 로 동작 (SKILL.md 설명에 트리거 문구). 도구 = 그 폴더의 `be.py init|make|check|render` · 사용법.md · 사용_안내.png
  - 합치는 법 : GitHub 에서 `https://github.com/4xrcompany-rgb/exhibitions/pull/new/feature/big-event-automation` 로 PR 만들어 머지, 또는 `git merge origin/feature/big-event-automation`
  - 필요한 것 : 파이썬 openpyxl · Pillow(➋_도구설치.bat 에 이미 있음) · 옛 .xls 엑셀이면 xlrd(be.py 가 없으면 자동 설치 — ➋_도구설치.bat 의 pip 줄에 `xlrd` 추가도 권장) · Playwright(기존 스킬 scripts/node_modules 사용)
  - **공통 파일 CDN 업로드(한 번)** : `engine/big_event.css` → `…/tgreen09/2026/00/bigevent/css/` · `engine/big_event.js` → `…/2026/00/bigevent/js/` (첫 대형 기획전 전에)
  - 검증 : 추석 엑셀 → make → render 가 실제 추석 페이지와 섹션 높이까지 같음 · 섹션 뺀 엑셀(xlsx)도 오류 0 · 다른 폴더에 복사해서도 init/make 동작
