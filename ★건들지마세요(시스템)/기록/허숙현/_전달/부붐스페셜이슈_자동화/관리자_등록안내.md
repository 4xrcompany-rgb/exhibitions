# 「부붐 스페셜이슈 스타일」 자동화 도구 — 관리자 등록 안내

요청: 허숙현 (2026-09-18) · 목적: **다른 직원 PC 에서도** 「부붐 스페셜이슈 스타일 시작해줘」로 같은 기획전을 만들 수 있게.
작업자는 `기록/<이름>/` 밖을 고칠 수 없어서, 이 폴더로 전달합니다.

## 1. 도구 옮기기
이 폴더(`기록/허숙현/_전달/부붐스페셜이슈_자동화/`)의
`ss.py · render.mjs · template.html · 사용법.md` 를
→ `.claude/skills/gihoekjeon-automation-system/scripts/부붐스페셜이슈/` 로 이동

- 이 위치면 Playwright(`scripts/node_modules`)를 도구가 자동으로 찾습니다. 다른 위치여도 상위 폴더를 올라가며 찾습니다.
- 옮긴 뒤 `기록/허숙현/_전달/` 은 지워도 됩니다.

## 2. 트리거 등록 (SKILL.md 또는 CLAUDE.md 에 추가)

```markdown
### 부붐 스페셜이슈 스타일 (레이아웃 고정형 스페셜이슈)
- 트리거: 「부붐 스페셜이슈 스타일 시작해줘」
- 도구: `scripts/부붐스페셜이슈/ss.py` — 같은 폴더 `사용법.md` 를 그대로 따른다
- 순서: `init <회차폴더>` → 설정.json(비주얼·브랜드소개·엔딩 문구, 칸별 상품번호만) → 이미지(파일명·크기 고정, 칸 옆 `_02`·`_03` = 자동 스와이프) → `make` → `check` → `render` (게시글 올린 뒤엔 `render <폴더> "<게시글 주소>"`)
- 납품: `<기획전명>_비즈호스트_붙여넣기.html` — 기존 본문을 지우고 교체
- 상품명·가격·게시글 번호는 설정에 쓰지 않는다 (상품명 = 몰 자동, 가격 = 페이지 실시간, 글번호는 매번 바뀜)
- 금지: `template.html` 의 `.tip{height:auto}` 삭제 — 게시판 전역 `a{height:100%}` 때문에 상품 정보가 좌측 상단으로 튐
```

## 3. 레이아웃 PSD (57MB · 저장소 제외)
허숙현 PC `E:\작업\스페셜이슈\_부붐스페셜이슈_자동화\레이아웃\fullpage.psd`
→ 공유 위치(NAS 등)에 두고 필요한 사람이 도구 폴더 `레이아웃/fullpage.psd` 로 복사.
PSD 가 없어도 도구는 동작합니다(`init` 이 건너뛰고, 이미지 규격표대로 만들면 됨).

## 확인된 것
- 0918 부붐 26FALL 값으로 회귀 시험: 생성 코드 = 손으로 만든 index.html (상품명 16개 몰 값 일치)
- `check` 이미지 17장·상품 10종·코드 규칙 통과 / `render` 임시 페이지·게시글 주소 × PC·모바일 통과
- 다른 위치(4XR 폴더 밖)·PSD 없이 복사해서 init → make → render 통과
