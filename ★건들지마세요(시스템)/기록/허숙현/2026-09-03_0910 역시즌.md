---
작업자: 허숙현
기획전: 0910 역시즌
날짜: 2026-09-03
상태: 진행중
소요: 50m + 2h10m
단계:
  구성표: 아직
  HTML: visual_section + sub01_section 구현 완료 · 상품영역 대기
  배너: 아직
오류:
---

> 👤 [[허숙현]]  ·  🗂 [[0910 역시즌]]  ·  🏠 [[맵]]

## ✅ 잘된 점

### 2026-09-03 — 참고 실측 · 레이아웃 시안

- 원본 엑셀(`E:\작업\콘텐츠\0910_역시즌\0910_역시즌 기획전_황다빈.xlsx`) 파싱 완료.
  - 메인 타이틀 `역대급 역시즌 쇼핑 찬스` / 기간 09.10-09.20 / 에디터 DB
  - 내용 `겨울에서 온 택배` + 본문 4줄
  - 영문 이미지 카피: `SURPRISE` · `WINTER, DELIVERED.` · `AN UNPRECEDENTED SHOPPING OPPORTUNITY YOU'LL NEVER SEE AGAIN` · `0910(THU) - 0920(SUN)`
  - 참여브랜드 12개 · 최대 할인율 71%
- 임베드 참고이미지 2장 추출(엑셀 drawing2 → sheet2 [기획전] 탭):
  ① 크래프트 택배박스 목업(레드/블루 실크스크린·송장 라벨·바코드) ② 봉투에서 옷 쏟아지는 콜라주.
- 참고링크(`read.php?index_no=21814` SUMMER IN BLACK) 인앱 브라우저로 **직접 실측** —
  하우스 골격 확보: `<link>Pretendard` + 게시판 틀 초기화 → `.img_memo_wrap` sticky(PC top:-1px / M top:100px, z:99)
  → `.discount_view .goods_list #goodsList1~3` (`.tit_40`/`.txt_46` 배경이미지 + grayscale→컬러)
  → 커스텀 히어로 + `.content_box`(h2/h4) → `@media 1940` / `@media 768`.
  진열 방식은 **(A) 게시판 직접진열**.
- 레이아웃 시안 `0910_역시즌_기획전.html` 제작. 택배박스가 열리며 옷이 쏟아지는 KV(CSS 크래프트 박스 + 플랩 오픈 + 순차 spill).
- Playwright PC(1440)·모바일(375) 실측 검증: 가로 넘침 0, `.rv` 등장 4/4, 요소 겹침 없음.
  모바일 KV 1066px 기준으로 라벨(50-208) / 71%(233-315) / 옷(336-636) / 박스(648~) / 날짜(1009-1040) 구간 분리 확인.

### 2026-09-04 — 디자이너 PSD 확정 → visual_section 구현

- 디자이너가 `img/top/img_top_pc.psd`(1920×750) 확정. **작업 파일이 `E:\작업\콘텐츠\0910_역시즌\index.html` 로 바뀜.**
  (09-03 의 `0910_역시즌_기획전.html` 은 CSS 로 박스를 그린 시안 — PSD 기반으로 다시 만듦)
- psd-tools 로 레이어 트리·타입 지표(폰트·크기·자간·변형행렬)·좌표를 전부 추출해 소재 19종 슬라이스.
  - `img_top_bg_pc.jpg`(1920×669, 플로우 바 사이 구간) · `img_flow_bar.png`(1920×41) · `img_flow_star.png`(19×19)
  - `img_pd01~13.png` · `img_top_box.png`(666×596) · `img_top_star_shape.png`(336×171)
- **타이포는 이미지가 아니라 코드**(사용자 지시). Anton(구글폰트) 150px `#2050b0` + Pretendard.
  Playwright 로 잉크 박스를 실측해 PSD 글리프 좌표와 대조 → 자간·좌표 보정. 글자별 x·폭 오차 0~2px.
- 애니메이션 3종 구현 — 플로우 바(상단 L→R / 하단 R→L, 검정 바는 고정) · 타이포 순차 → 상품 13컷 →
  img_box 하단에서 상승 → ★_text 등장 후 무한 깜빡임.
- Playwright 검증: PC 1920 · 모바일 768 양쪽 콘솔에러 0, 깨진 이미지 0, 가로 넘침 0.
  프레임 추적으로 순서 확인 — 텍스트 0~1.05s → 상품 1.2~1.95s → 박스 2.1s → 별 2.4s 후 점멸.
  PSD 합성본 대비 픽셀 차 평균 11.89(=글리프 안티에일리어싱·JPEG 링잉 수준).

### 2026-09-04 (2차) — CDN 연결 · 모바일 시안 · sub01_section

- 이미지 경로를 **CDN 공통 경로 한 줄**로 통일 —
  `https://cdn-tgreen.bizhost.kr/phpskr/tgreen09/2026/09/10/HSH/img/top/` + 파일명.
  CDN 실제 파일과 로컬 추출본이 **바이트 단위로 일치** 확인(bg/바/별/상품/박스 5종 대조).
- **모바일 750×900 시안**(바 56 + 스테이지 788 + 바 56). PC 스테이지와 별도 구성으로 `.pc_ey`/`.mo_ey` 토글.
  Anton 82px 3줄 + 괄호 안 상품 3컷(인라인 갭) + 박스 중앙 + SEASON OFF 우상단 + 좌우 플랭킹 3컷.
  상품컷은 비율이 제각각이라 **폭이 아닌 높이(1.25em) 기준**으로 통일.
- flow_bt 텍스트 **좌우 반전 유지**로 확정(사용자 확인) — `.flow_bt .flow_set h5 { transform: scaleX(-1) }`.
- 전역 폰트를 `'Pretendard Variable', Pretendard, sans-serif` 로 수정(사용자 확정).
- **sub01_section** 을 참고본(read.php?index_no=21862)의 `content_section` 구조로 구현 —
  h3 타이틀 / h5 기간+`.date_status`(D-n·D-DAY·종료 자동 계산, 노랑 #fde732) / h4 본문, nth-child 순차 등장.
  카피는 원본 엑셀에서 읽음(타이틀 `겨울에서 온 택배`, 기간 09.10-09.20, 본문 4줄).
- 검증: 1920 / 750 / 768 세 폭 모두 콘솔에러 0 · 깨진 이미지 0 · 가로 넘침 0.
  750 폭에서 visual_section 정확히 **750×900**, 뱃지 `D-6`(오늘 09-04 기준).

### 2026-09-04 (3차) — 배경 규격 정리 · 모바일 양쪽 정렬 · 점멸 제거

- **플로우 바를 배경 위에 얹는 구조로 변경**(absolute top/bottom). 그래서 배경이 전체 캔버스를 덮는다 —
  PC `img_top_bg_pc.jpg` **1920×750**(PSD 원본 캔버스와 동일) · 모바일 `img_top_bg_m.jpg` **750×900**.
  덤으로 PC 좌표가 **PSD 절대좌표와 그대로 일치**하게 정리됨(예전엔 바 높이 41 만큼 빼서 썼음).
- 모바일 타이포 3줄을 **좌 43 · 우 707 양쪽 라인에 정렬**. 글자크기는 78px 로 통일하고
  줄마다 **갭(괄호 안 상품자리) 폭을 다르게** 줘서 줄 끝을 맞췄다 — 1.683 / 1.053 / 1.848em.
  (자간으로 늘리면 줄마다 트래킹이 달라져 어색해짐. 갭은 원래 디자인 요소라 폭을 바꿔도 자연스러움)
  상품컷도 같은 43 / 707 라인 안으로 넣음(예전엔 화면 밖으로 물려 있었음).
- ★_text **점멸 제거**(사용자 요청). 순차 등장만 유지.
- 상품컷 **호버 부유** 추가(회전 없음) — 마우스를 올린 동안만 위아래 **18px**(기준폭 기준), 주기 **1.5s**.
  (처음 10px·3s 로 넣었더니 "너무 느리다" → 진폭 1.8배·주기 절반으로 조정)
  1.5s 는 **플로우 바가 한 칸 지나가는 시간(3.0s)의 절반**(실측: PC 292px / 97.5px·s = 3.0s).
  바 한 칸에 두 번 둥실거려 박자가 맞는다. 따로 노는 움직임이 안 되게 함. 축도 세로라 바(가로)와 겹치지 않고, 이징도 ease-in-out(바는 linear)으로 분리.
  ★**CSS `:hover` + `animation` 으로 하면 호버를 뗄 때 떠 있던 높이에서 0 으로 툭 떨어진다**
  (애니메이션 제거는 transition 을 발동시키지 않음 — 실측 -9.3px → 0 즉시 점프).
  그래서 Web Animations API 로 걸고, 뗄 때 현재 위치에서 제자리로 420ms 되돌린다.
  터치 기기는 `matchMedia('(hover: hover)')` 로 걸러 아예 걸지 않는다.
  ※ `var CAN_HOVER` 를 `buildVisual()` 호출보다 아래에 두면 호이스팅으로 undefined 라 전부 무효화됨(한 번 밟음).
- 상품 진열 영역은 **A방식(게시판 직접진열)** 로 확정 — 건드리지 않음.
- 검증: 1920 폭에서 visual_section 정확히 **1920×750**, 750 폭에서 **750×900**.
  PC 는 좌표계를 바꾼 뒤에도 PSD 대비 픽셀 차 평균 11.44 로 동일(안티에일리어싱 수준).

## ⚠️ 막힌 점 · 오류

- ★**CDN 재업로드 필요 2건** — `img_top_bg_pc.jpg`(669→750 으로 바뀜) · `img_top_bg_m.jpg`(신규).
  나머지 소재는 CDN 과 로컬이 일치.
- 원본 엑셀이 열려 있는 상태(`~$` 임시파일 존재). 읽기만 하고 쓰지 않음. (§7-1)
- **Anton 버전 차이** — 포토샵 설치본보다 구글폰트 Anton 의 괄호 `( )` 가 세로로 16px 더 길다(147 vs 131).
  괄호만 `.txt_par { transform: scaleY(.891) }` 로 보정. 글자 폭·위치는 원본과 동일.
- 모바일 배경은 PC 배경(`img_top_bg_pc.jpg`)을 750×788 로 늘려 쓴다(세로 그라데이션이라 무리 없음).
  화질이 아쉬우면 `img_top_bg_m.jpg` 를 따로 뽑아 올리면 된다.
- 모바일은 13컷 중 **6컷만** 노출(인라인 3 + 플랭킹 3). 나머지는 화면이 좁아 뺐다 — 교체는 `MO_STAGE.PD` 배열에서.
- 아직 승인 전이라 게시판 납품본(fragment) 변환 · 구성표 엑셀 · 피그마 규격 레이아웃 · 배너는 미착수.
- 미사용 소재(텍스트 PNG 7장)와 편집 전 백업은 `E:\작업\콘텐츠\0910_역시즌\_to_delete\` 로 옮김. (§7-6)

## 💡 개선 요청 (중앙에 전달)

- 없음
