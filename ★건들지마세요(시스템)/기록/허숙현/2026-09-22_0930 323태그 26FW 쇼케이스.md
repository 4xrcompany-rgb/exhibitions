---
작업자: 허숙현
기획전: 0930 323태그 26FW 쇼케이스
날짜: 2026-09-22
상태: 부분성공
소요: 150m
단계:
  구성표: 건너뜀        # 기존 폼(26spring index.html) 콘텐츠 교체 작업 — 구성표 미생성
  HTML: ok
  배너: 건너뜀
오류:
  - 유형: 기타
    내용: 엑셀에 없는 값 — 룩북 게시글 번호(LOOKBOOK_NO 비움→링크 자동 숨김)·에디토리얼 12컷↔상품 매핑. 가격은 2차에서 상품페이지 자동 로드로 전환(셋업 옵션가 이슈는 몰 표기 그대로 따름)
    해결: 미해결 — 사용자 확인 필요(보고서에 명시)
---

> 👤 [[허숙현]]  ·  🗂 [[0930 323태그 26FW 쇼케이스]]  ·  🏠 [[맵]]

## 작업 내용

- 폴더: `E:\작업\룩북&쇼케이스\323태그\26fall`
- 기획 엑셀 `0930_323tag 26fw 가을 쇼케이스_김정희.xlsx` 4시트(쇼케이스·상품번호 등) → `read_workbook.py` 로 파싱
- 요청: ① 엑셀 내용을 기존 `index.html`(26spring 폼) 에 반영 ② 메인 비주얼 이미지 → 영상 코드로 교체(영상은 추후 업로드)
- 참고무드: 게시글 21364 (= 26spring 쇼케이스, 기존 폼 그대로) · 에디토리얼 제품컷 섹션 유지

## ✅ 잘된 점

- 기존 26spring 폼 유지, 엑셀 텍스트만 교체 · 메인 비주얼 img→video(pc/m 2본, poster 유지) · 이벤트 2→3열 · 쿠폰 idx 874/10%/마감 10-07 · 상품 14개(플랫컷 _1, 692695만 _2)
- Playwright PC1920·M750 렌더 OK, script 엔티티 0건, node --check 저장전/후 통과
- 2차(사용자 요청 3건): ① 소재 공통 경로 `PATH` 상수 1개 + `data-src/data-poster` 로 JS 주입 ② 상품 리스트 = 번호만 → 상품페이지 fetch(og:title·.none_op1 컬러·.price_01 정상가·sale_price meta·og:image) 할인가/정상가취소선/할인율 표기(0818 FALL 로더 참고) ③ 관리 데이터(PATH·쿠폰 idx/마감·룩북번호·이벤트상품·EDITORIAL·PRODUCTS) 스크립트 최상단 집중
- 3차 이벤트 영역 리디자인: 시안 A~W 총 23안 렌더 제시 → 사용자 선택 T(무신사·29CM 리스트형)+T3(버튼 안 카운트다운)+W2(하단 WINNER 행). 2·3번 이벤트를 '댓글 이벤트' 하나로 통합(버튼 2개), 유의사항 우측 텍스트 링크→기존 모달 재사용, WINNERS 관리데이터(비면 행 제거). PC1920·M750·당첨자 유무·모달 열림 렌더 검증 OK
- 로컬 fetch 검증: file:// 는 fetch 불가 → http.server + Playwright route 로 실제 몰 상품페이지 물려 14개 전부 정상 파싱 확인

## ⚠️ 막힌 점 · 오류

- 시안 CSS 를 `.var_x` 클래스로만 쓰면 원본 `#event_section` id 규칙에 밀림 → 시안은 `#event_section.var_x` 로 격상해야 함
- 모달 블록을 문자열 검색으로 잘라 옮길 때 들여쓰기 다른 `</div>` 가 부분 매칭돼 닫는 div 누락 → 섹션이 뒤 섹션을 삼킴. 마크업 이식 후 반드시 `<div`/`</div>` 개수 균형 검사

- 엑셀 첫 파싱(openpyxl) 1.5MB 이미지 포함으로 120s 타임아웃 → read_workbook.py 로 바로 해결
- bash heredoc 으로 한글 python 스크립트 넘기면 cp949 깨짐 → 스크립트 파일로 저장 후 `python -X utf8` 실행

## 💡 개선 요청 (중앙에 전달)

-
