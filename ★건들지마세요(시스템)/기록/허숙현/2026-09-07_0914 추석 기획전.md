---
작업자: 허숙현
기획전: 0914 추석 기획전
날짜: 2026-09-07
상태: 진행중
소요: 50m
단계:
  구성표: 건너뜀      # 원본 기획엑셀(정용진) 직접 사용
  HTML: ok
  배너: 미착수
오류:
  - 유형: 게시판저장(엔티티)
    내용: index.html <script> 안에 HTML 엔티티 리터럴 8건(&amp; &lt; &gt; &quot; &#39;)이 그대로 있었음. 게시판 저장 시 디코드되어 escapeAttr 문법이 깨지고 페이지 전체가 죽는 SKILL §7-9 사고 조건. 기존 파일에 있던 문제.
    해결: \u0026 유니코드 이스케이프로 교체. node --check 로 '저장 후' 시뮬레이션까지 통과 확인(엔티티 0건).
---

> 👤 [[허숙현]]  ·  🗂 [[0914 추석 기획전]]  ·  🏠 [[맵]]

## 작업 위치
`E:\작업\콘텐츠\0914_추석 기획전\index.html` (0615 블랙프라이데이 완성본을 추석용으로 개편)
엑셀: `0914_추석 기획전★_정용진.xls` (BIFF8 .xls — openpyxl 불가, **xlrd 2.0.1** 로 파싱)

## 처리한 요청 4건
1. **비주얼 영상 삭제 → 임시 이미지** — `.cont_video_pc/_m`·사운드버튼·시작안내·`initVisualSound()`(134줄) 전부 제거.
   `BF_CONFIG.visual = {pc, mobile, alt}` 로 이미지 1곳 관리, `renderVisualImages()` 가 `<img>` 렌더.
   임시경로 `.../2026/09/14/HSH/img/top/img_top_pc.jpg` · `img_top_m.jpg` (업로드 후 이 2줄만 교체).
2. **FD Carimboh → Pretendard** — cdnfonts 링크 제거, `title_con h1` 폰트지정 삭제, 정의 안 된 `--bf_title_font`(무효 선언) 6곳 정리.
3. **자동화 상품** — 엑셀 [기획파일] `고모의 신상 참견`→`daily_01`, `이모의 가격 참견`→`daily_02`.
   각 **14일(9.14~9.27) × 12개 = 168개, 중복 0**. 연동 필수라 `eventStart/End`·기간표기도 9.14~9.27 로 변경.
4. **다크 → 라이트** — 색·그림자·질감만 교체(레이아웃/구조/문구 무손상). 한지 아이보리 `#fbf6ec` + 잉크 `#241f19` + 홍시 `#c8402c` + 금 `#a8813f`. 청록 계열(`#aac4c2`/`rgba(170,196,194,*)`) → 금 계열. 사진 위 요소(품절뱃지·툴팁·붉은 버튼 흰글씨)는 그대로 유지.

## ✅ 잘된 점

- 색 190종을 전부 열거해 매핑표로 일괄 변환 → 누락 없이(잔여 다크값 0) 레이아웃 무손상.
- Playwright PC 1920 / 모바일 768 풀페이지 렌더 검증. 두 폭 모두 데일리탭 14개·카드 12개·video 0·JS 에러 0(`$` 미정의는 게시판이 jQuery 제공하므로 정상).

## ⚠️ 막힌 점 · 오류

- **프래그먼트를 로컬에서 열면 quirks 모드**라 KV 자리가 1200px로 부풀어 보임. doctype 있는 표준모드(=실제 게시판)에서는 `min-height:320px` 로 정상. 코드 문제 아님.
- **미반영(요청 밖이라 손대지 않음)** — 쿠폰 8종이 6월 날짜/idx(801~808) 그대로, 상품권·럭키백 상품번호·이미지가 6월 것, 섹션 제목/카피가 블랙프라이데이 문구. 엑셀에 추석 값(쿠폰 idx 868/867, 럭키백 692836, 상품권 692833~5, 카피 전문) 다 있음 → 지시 오면 반영.
- **기존 버그 발견** — `initMobileStickyClone()` 이 만든 클론이 PC에서 CSS 없이 body 맨 아래 파란 링크로 노출됨(`.bf_mobile_sticky_clone` 스타일이 `@media(max-width:768px)` 안에만 있음). 원본에서도 재현. 요청 밖이라 미수정, 보고만.

## 💡 개선 요청 (중앙에 전달)

- 이번처럼 **이전 기획전 완성본을 재활용**할 때, `<script>` 엔티티(§7-9)·기간·쿠폰 idx 는 매번 같은 자리에서 걸린다. 재활용 시작 시 자동 점검하는 체크 스크립트가 있으면 좋겠음.
- 기획엑셀이 구형 `.xls`(BIFF8)라 openpyxl 이 못 읽는다. 스킬 `read_workbook.py` 에 xlrd 폴백 추가 필요.
