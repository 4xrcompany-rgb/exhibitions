# 4XR 기획전 작업 폴더

이 폴더는 **4XR 쇼핑몰 기획전 페이지와 배너**를 만드는 작업 공간이다.

기획전·배너 관련 요청(“기획전 만들어줘”, “구성표 만들어줘”, “엑셀 반영해줘”,
“배너 만들어줘”)이 오면 **`gihoekjeon-automation-system` 스킬을 먼저 읽고 그대로 따른다.**
순서·규칙·주의사항이 전부 거기 있다. 임의로 순서를 바꾸지 않는다.

---

## 항상 지킬 것

**① 사용자 지시 범위를 넘지 않는다**
「위에 이 블록 넣어줘」는 그 블록만 넣으라는 뜻이다.
디자인·섹션 구성·색·타이포·문구를 **요청 없이 바꾸지 않는다.**
「깔끔하게 해달라」는 말도 갈아엎으라는 뜻이 아니다. 무엇을 뺄지 먼저 확인한다.

**② 엑셀은 절대 새로 만들어 덮어쓰지 않는다**
구성표 엑셀은 사용자가 채운 값이 들어 있다. 항상 **읽어서 수정**한다.
사용자가 “수정했다”고 하면 파일이 열려 있는지·저장했는지부터 확인한다.

**③ 빈 구좌는 숨기지 않고 없앤다**
`사용=N` 이거나 내용이 비면 **마크업 자체를 만들지 않는다.**
`display:none` 이나 빈 `<div>` 를 남기지 않는다.

**④ 파일을 지우지 않는다**
지워야 할 것 같으면 같은 폴더의 `_to_delete/` 로 옮기고 사용자에게 알린다.

**⑤ 만든 결과는 눈으로만 확인하지 않는다**
HTML 은 Playwright 로 **PC·모바일 두 폭** 렌더 테스트를 돌린다.
애니메이션은 중간 프레임도 캡처해 실제로 순차 등장하는지 본다.

---

## 폴더 규칙

```
기획전_제작/<기획전명>/
├─ <원본 기획 엑셀>.xlsx
├─ 기획전_구성표.xlsx        ← 스킬 ③단계에서 생성
├─ <기획전명>_기획전.html     ← 스킬 ⑤단계에서 생성
├─ 배너/                     ← 완성 배너 JPG 7장
├─ _소재/                    ← 섹션별 소재
└─ _to_delete/               ← 지울 파일 임시 보관
```

---

## 몰 기본값

| | |
|---|---|
| 몰 | https://www.4xr.co.kr (모바일 `m.4xr.co.kr`) |
| 상품 CDN | `https://cdn-tgreen.bizhost.kr/files/goods/` |
| 상품 링크 | `https://www.4xr.co.kr/shop/view.php?index_no={코드}` |
| 브랜드 링크 | `https://www.4xr.co.kr/shop/brand_goods.php?brand={id}` |
| 기획전 게시판 | `https://www.4xr.co.kr/bbs/read.php?index_no={번호}&boardid=issue` |
| 폰트 | SB 어그로 (`.woff` 만 있음 — `.woff2` 는 404) |

기획전 HTML 은 게시판 본문 안에 들어간다. 맨 위 **고정 블록**은 스킬 §5-A 참조.

---

## 도구

- **피그마** — 배너 레이아웃·디자인. Full seat 필요 (View seat 은 편집 불가)
- **힉스필드** — 질감·배경 이미지 생성, 누끼

두 MCP 는 `.mcp.json` 에 설정되어 있다. 처음 실행 시 `/mcp` 로 로그인한다.

---

## 상세 문서

- 스킬 본문 → `.claude/skills/gihoekjeon-automation-system/SKILL.md`
- 전체 기록(스크립트 전문 포함) → `★건들지마세요(시스템)/참고/기획전_작업_자동화_마스터.md`
